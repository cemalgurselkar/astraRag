services:
  astrarag:
    build:
      context: .
      dockerfile: Dockerfile

    image: astrarag:0.1.0
    container_name: astrarag

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./data:/app/data

    restart: unless-stopped